# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/
.env/

# MLflow
mlflow.db
mlruns/

# Streamlit deployment — keep these tracked
!model.joblib
!clean_demand_data.csv

# Data (large raw files) — ignore CSVs at root level only
*.csv
!data/
!data/raw/
!data/data_processed/
!data/raw/*.csv
!data/data_processed/*.csv
*.ipynb_checkpoints

# Security — never commit private keys
*.pem

# IDEs
.vscode/
.idea/